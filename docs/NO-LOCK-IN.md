# No lock-in

Articles, authors, media, redirects, configuration, and generated output are ordinary files. Git is the revision ledger. Reader delivery is static and does not require a TAHAI account, analytics service, database, or optional Worker.

Launch Desk exports a portable package and applies it only with explicit local confirmation. Optional services must remain additive and cannot become the authority for public content.
