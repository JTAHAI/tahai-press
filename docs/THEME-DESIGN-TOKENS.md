# Theme design tokens

Every package declares background, surface, text, muted text, link, accent, focus, success, warning, error, headline/body/utility font stacks, reading measure, spacing, card radius, rule width, image treatment, and print background tokens. Core accessibility rules retain precedence when a theme creates an unsafe contrast or focus condition.
