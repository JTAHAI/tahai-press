# Maps Desk

Map records in `content/maps/` contain only explicit publisher-supplied locations. The static output prioritizes an accessible ordered location list, address, description, source, attribution, methodology, limitations, print view, and no-JavaScript reading. It never infers a location from article text.

An interactive map enhancement is optional; every mapped fact must already be understandable from the static page before one is considered.
